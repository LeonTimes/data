# 数据爬取

Python 示例：httpx + BeautifulSoup + lxml。

## 环境

```bash
cd "/Users/liyucheng/Desktop/数据爬取"
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## 运行

```bash
python -m src.scrape
python -m src.scrape "https://example.com"
```
